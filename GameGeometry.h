#pragma once
#include <raylib.h>

typedef struct {
	Vector3 pos;
	float width;
	float height;
	float length;
	Color color;
} Cube;

typedef struct {
	Vector3 pos;
	float width;
	float height;
	float length;
	Color color;
} Plate;